name = "wc2026-results"
main = "index.js"
compatibility_date = "2024-11-01"

# FD_TOKEN is set as a secret, not here:
#   npx wrangler secret put FD_TOKEN
#
# Optional: override the competition code (defaults to "WC")
# [vars]
# FD_COMPETITION = "WC"
